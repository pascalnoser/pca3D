% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pca3d.R
\name{plot_pca3d}
\alias{plot_pca3d}
\title{Create a static 3D-style PCA plot}
\usage{
plot_pca3d(
  pca,
  dims = 1:3,
  metadata = NULL,
  color_by = NULL,
  var_explained = TRUE,
  theta = 30,
  phi = 20,
  depth_cue = TRUE,
  point_size = 3,
  axes = c("none", "full", "gizmo"),
  axis_color = "grey40"
)
}
\arguments{
\item{pca}{A \code{prcomp} object.}

\item{dims}{Numeric vector of length 3 giving which principal components
to use, e.g. \code{1:3} (the default) or \code{c(1, 2, 4)}.}

\item{metadata}{Optional data frame of sample metadata with one row per
sample in \code{pca}, used to colour points via \code{color_by}. If both \code{pca$x}
and \code{metadata} have row names, rows are matched by name; otherwise they
are assumed to be in the same order.}

\item{color_by}{Optional column in \code{metadata} to colour points by, as an
unquoted name (e.g. \code{treatment}) or a string (e.g. \code{"treatment"}).
Numeric columns are treated as continuous, others as categorical; no
colour scale is applied automatically so you can add your own, e.g.
\code{plot_pca3d(pca, metadata = md, color_by = group) + scale_color_brewer(palette = "Set1")}.}

\item{var_explained}{Logical; if \code{TRUE} (default), add a caption listing
the percent variance explained by each of \code{dims}.}

\item{theta}{Azimuth rotation angle in degrees for this view. Default \code{30}.}

\item{phi}{Elevation tilt angle in degrees. Default \code{20}.}

\item{depth_cue}{Logical; if \code{TRUE} (default), points closer to the
viewer are drawn larger and more opaque.}

\item{point_size}{Base point size. Default \code{3}.}

\item{axes}{Draw an indicator of which direction each PC axis points.
One of \code{"none"} (default), \code{"full"} (arrows through the point cloud
from the origin along each rotated PC direction, labelled \verb{PC<n>} at
the tip), or \code{"gizmo"} (the same arrows, but short and anchored at a
fixed corner of the plot, independent of data scale, like a small
rotating compass).}

\item{axis_color}{Colour used for the axis arrows/labels when \code{axes != "none"}. Default \code{"grey40"}. This is a fixed colour, not mapped to
data, so it won't interfere with \code{color_by}'s colour scale.}
}
\value{
A \code{ggplot} object.
}
\description{
Projects three principal components onto 2D after applying a fixed 3D
rotation, giving a single "camera angle" view of the point cloud. Points
closer to the viewer can optionally be drawn larger/more opaque
(\code{depth_cue}) to reinforce the 3D effect. Use \code{\link[=animate_pca3d]{animate_pca3d()}} to render
a full rotating GIF instead of a single view.
}
\details{
Because the 2D x/y axes after rotation are a blend of the selected PCs
rather than a single PC, axis titles are omitted. When \code{var_explained = TRUE}, a caption lists the percent variance explained by each of \code{dims}
instead.
}
\examples{
pca <- prcomp(iris[, 1:4], scale. = TRUE)
plot_pca3d(pca, metadata = iris, color_by = Species)
plot_pca3d(pca, metadata = iris, color_by = Species, axes = "gizmo")
}
